{
  "name": "flex-business-hours",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.5.0"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "moment-timezone": "^0.5.31",
    "twilio": "^3.49.1"
  }
}
